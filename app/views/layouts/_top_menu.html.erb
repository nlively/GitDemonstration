<nav id="top-menu">
    <span class="inner">

        <% if current_user.nil? %>
              <%= link_to "Sign In", new_user_session_path, :id => 'sign-in' %>

              &nbsp;|&nbsp;

              <%= link_to "Create an Account", new_user_registration_path, :id => 'new-account' %>
        <% else %>

              <%= link_to "Sign Out", destroy_user_session_path, :method => :delete, :id => 'sign-out' %>
        <% end %>
      </ul>
    </span>
</nav>

